// Debug vs Release builds

// Debug - a debug build emits the complete symbolic debug information to help while debugging

// Release - The symbolic info is not emitted and the code execution is optimized
    // The final executable size is lesser than the debug executable
